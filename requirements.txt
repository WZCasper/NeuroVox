# NeuroVox — зависимости
# Установка:  pip install -r requirements.txt
#
# Требуется Python 3.10 – 3.12 (64-бит) для Windows 11.
# Версии заданы диапазонами, чтобы установка не ломалась на разных версиях Python.
#
# ВАЖНО: у видеокарт AMD нет CUDA, поэтому всё считается на процессоре.
# Обычный «pip install torch» под Windows ставит именно CPU-сборку — то, что нужно.

# --- Интерфейс ---
customtkinter>=5.2.0

# --- Захват экрана и обработка изображений ---
mss>=9.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# --- Распознавание текста (OCR) ---
# EasyOCR — основной движок (работает «из коробки», без установки лишних программ).
easyocr>=1.7.1
# pytesseract — необязательный быстрый запасной вариант (нужен ещё установленный Tesseract-OCR).
pytesseract>=0.3.10

# --- Озвучка (TTS, Silero) и воспроизведение ---
# torch — нужен и для Silero, и для EasyOCR. Только CPU-режим.
torch>=2.1.0
sounddevice>=0.4.6

# --- Фильтрация повторов ---
rapidfuzz>=3.0.0
